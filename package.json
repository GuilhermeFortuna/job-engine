{
  "name": "job-engine",
  "private": true,
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "dev": "pnpm -r --parallel --if-present run dev",
    "check": "pnpm -r --if-present run check",
    "test": "pnpm -r --if-present run test",
    "build": "pnpm -r --if-present run build",
    "ci": "./scripts/ci.sh",
    "ci:local": "./scripts/ci.sh"
  }
}
