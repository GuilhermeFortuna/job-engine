{
  "name": "@job-engine/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "tsc && electron dist/main/index.js",
    "check": "tsc --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run --project unit --project runtime --project forms",
    "test:fixtures": "tsc -p tsconfig.test.json && node scripts/run-fixtures.mjs",
    "build": "tsc"
  },
  "dependencies": {
    "electron": "43.2.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/jsdom": "^30.0.0",
    "@types/node": "24.10.0",
    "@types/node-forge": "^1.3.14",
    "jsdom": "30.0.1",
    "node-forge": "^1.4.0",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
