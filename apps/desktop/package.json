{
  "name": "@job-engine/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "tsc && electron dist/main/index.js",
    "check": "tsc --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run tests/unit",
    "test:fixtures": "tsc -p tsconfig.test.json && vitest run tests/fixtures",
    "build": "tsc"
  },
  "dependencies": {
    "electron": "43.2.0"
  },
  "devDependencies": {
    "@types/node": "24.10.0",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
